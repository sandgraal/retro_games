# Supabase credentials for local development.
# Copy this file to `.env`, fill in the values, and run `npm run build:config`.
# The generated `config.js` remains gitignored – do not commit the populated file.
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
# Optional: override the Supabase table(s) used for fetching games.
# Provide a single table via SUPABASE_TABLE or a comma-separated list with SUPABASE_TABLES.
# When unset, the client will try `games`, then `games_view`, then `games_new`.
# SUPABASE_TABLE=games_new
# SUPABASE_TABLES=games_new,games
# Optional: tune streaming/aggregate behavior (defaults shown)
# SUPABASE_STREAM_PAGE_SIZE=400
# SUPABASE_RPC_GENRES=rpc_genre_counts
# SUPABASE_RPC_TIMELINE=rpc_timeline_counts

# eBay Finding API for price valuations (median sold listings)
# Obtain credentials from https://developer.ebay.com/
EBAY_APP_ID=your-ebay-app-id
EBAY_DEV_ID=your-ebay-dev-id
EBAY_CERT_ID=your-ebay-cert-id
EBAY_GLOBAL_ID=EBAY-US
EBAY_REFRESH_HOURS=24
# Service role key is required for scripts that insert price snapshots.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Media storage buckets used by Supabase Storage.
# Buckets must exist (migration creates defaults) and match the moderation workflow.
SUPABASE_STORAGE_PUBLIC_BUCKET=game-covers
SUPABASE_STORAGE_AUTH_BUCKET=media-auth
SUPABASE_STORAGE_ARCHIVE_BUCKET=media-archive
SUPABASE_STORAGE_PENDING_BUCKET=media-pending
# Optional: override the CDN base for storage assets.
# SUPABASE_STORAGE_CDN_URL=https://your-project-id.supabase.co/storage/v1
